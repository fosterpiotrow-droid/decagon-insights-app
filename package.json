{
  "name": "decagon-insights-app",
  "version": "1.0.0",
  "description": "Netlify serverless function for analyzing Decagon customer support conversations using Claude API",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
